# Audio recording
PyAudioWPatch>=0.2.12.8
numpy>=1.24.0

# Transcription (torch/torchaudio are pulled in by whisperx — don't pin them
# separately to avoid conflicting with existing installations)
whisperx>=3.1.1

# Summarization (Ollama HTTP API)
requests>=2.31.0

# Export
python-docx>=0.8.11
